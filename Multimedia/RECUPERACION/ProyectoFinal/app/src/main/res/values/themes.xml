<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProyectoFinal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="backgroundColor">@color/primary_color</item>
        <item name="buttonStyle">@style/Widget.Button.ProyectoFinal</item>
    </style>

    <style name="Theme.ProyectoFinal" parent="Base.Theme.ProyectoFinal" />

    <!-- Estilo personalizado para los botones -->
    <style name="Widget.Button.ProyectoFinal" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">8dp</item> <!-- Esquinas redondeadas opcionales -->
    </style>
</resources>
